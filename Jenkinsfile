pipeline {
    agent any
    stages {
        stage('Build'){
            steps {
                sh '''
                echo 'Install Terraform'
                sudo apt update
                '''
            }
        }
    }
}